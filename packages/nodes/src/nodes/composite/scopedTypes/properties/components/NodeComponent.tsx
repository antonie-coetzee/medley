import React, { useState } from "react";
import { TNodeComponent } from "@medley-js/common";
import { PropertiesNode } from "../PropertiesNode";

export const NodeComponent: TNodeComponent<PropertiesNode> = ({
  context,
  host,
  selected,
}) => {
  return (
    <div></div>
  );
};
