import { componentStory } from "@/util/util.sb";
import React from "react";
import { NodeContainer } from "../components";

export const Container = componentStory(async () => {
  return () => <div />;
});
