import { nodeStory } from "@/util/util.sb";
import { Meta } from "@storybook/react";
import { IfType } from "..";

export default {
  title: "Nodes/If",
} as Meta;

export const Node = nodeStory(IfType);
