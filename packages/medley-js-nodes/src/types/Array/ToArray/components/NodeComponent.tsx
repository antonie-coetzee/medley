import React from "react";
import { TNodeComponent } from "@medley-js/common";
import { ArrayNode } from "../node";

export const NodeComponent: TNodeComponent<ArrayNode> = (props) => {
  return (<></>);
};
