// import {ModelGraph} from "@medley/medley-mve"
import { action, makeAutoObservable, runInAction } from "mobx";

export class ModelGraphStore {
    //public modelGraph: ModelGraph | null = null;

    constructor() {
        //makeAutoObservable(this, {modelGraph:true, setModelGraph: action});
    }

    // public setModelGraph(modelGraph: ModelGraph){
    //         this.modelGraph = modelGraph;
    // }
}